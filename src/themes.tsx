export type ThemeType = {
  primary: string;
  secondary: string;
  fontFamily: string;
};

export const lightTheme: ThemeType = {
  primary: "#E6692E",
  secondary: "#EDEDED",
  fontFamily: "Poppins, sans-serif",
};
